server:
  port: 8080
  address: "localhost"
db:
  user: "root"
  password: "root"
  name: "auth"
  host: "auth-db"
logger:
  source: true
auth:
  secret: "123"
  accessLifetime: "24h"
  refreshLifetime: "120h"
client:
  address: "bot"
  retryCount: 3